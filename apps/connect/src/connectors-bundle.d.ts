// The native connectors of this release, generated into dist/ by build.ts:
// each one's manifest, and its MCP server bundled into one ES module.

declare const connectors: readonly { manifest: unknown; code: string }[];
export default connectors;
